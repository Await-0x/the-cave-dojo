mod DraftUtils {
    use traits::{TryInto};
    use dojo::world::Context;
    use DragonsNest::components::draft::{DraftChoice};

    fn get_random_card(ctx: Context, draft_id: usize, number: u8) {
    }
}